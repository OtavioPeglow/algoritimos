const prompt = require("prompt-sync")();

let numeros = [1, 2, 3];

console.log(numeros);

numeros[0] = numeros[0] * 2;
numeros[1] = numeros[1] * 2;
numeros[2] = numeros[2] * 2;

console.log(numeros)